{
  "tools": [
    {
      "type": "function",
      "function": {
        "name": "send_text_message",
        "description": "Send an SMS text message to the caller's phone number. Use this when the caller requests information to be sent via text, needs a confirmation, or asks for a follow-up message.",
        "parameters": {
          "type": "object",
          "properties": {
            "message": {
              "type": "string",
              "description": "The text message content to send to the caller"
            }
          },
          "required": ["message"]
        }
      }
    }
  ],
  "version": "1.0.0"
}
